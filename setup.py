from setuptools import setup, find_packages

setup(name='aip',
      version='0.0.1',
      url='',
      license='',
      author='',
      author_email='',
      description='',
      packages=find_packages(),
      zip_safe=False,
      setup_requires=[],
      install_requires=[
          'pandas',
          'numpy',
          ])

# sudo apt-get install libmysqlclient-dev -y
# sudo apt-get install gcc
# https://stackoverflow.com/questions/47144624/unable-to-install-uwsgi-with-pip-on-ubuntu-16-04

